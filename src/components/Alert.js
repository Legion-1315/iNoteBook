import React from "react";

const Alert = () => {
  return (
    <div>
      <div className="alert alert-primary" style={{fontWeight:'bolder'}} role="alert">
        FONT AWESOME !
      </div>
    </div>
  );
};

export default Alert;
